{
  "require": ["ts-node/register"],
  "extensions": ["ts"],
  "spec": ["tests/**/*.test.ts"],
  "timeout": 60000,
  "exit": true,
  "parallel": false,
  "reporter": "spec",
  "node-option": [
    "loader=ts-node/esm"
  ]
}
